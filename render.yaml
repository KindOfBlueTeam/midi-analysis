services:
  - type: web
    name: atmo-audio-tools
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "atmo_audio_tools.web:create_app()" --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
